# 100000 style match

Request: redo `100000` to look like `10000`.

Generated with the built-in image_gen tool using `public/emojis/10000.webp` as the visual reference. `generated.png` preserves the tool output; `candidate.webp` is a lossless encoding. `original.webp` archives the current production asset. The two 128px previews demonstrate the candidate on light and dark backgrounds.

Review: exact six-digit text (one 1 and five zeros), narrow slanted red brush lettering, two tapered underlines, and transparent background. Inspected both previews. User approved the exact candidate SHA-256 recorded in `manifest.json`; promoted to `public/emojis/100000.webp`. Original image and metadata are archived here.

## Prompt

Use case: text-localization. Create a replacement emoji using the attached 10000 emoji as the style reference. Exact text: "100000" (one 1 followed by FIVE zeros), all on one line. Match the reference narrow, tall, right-slanted red handwritten brush numerals, tapered stroke ends, dark vivid red color, and two separate sweeping red brush underlines (shorter upper underline and longer lower underline). Adapt spacing to fit six digits cleanly. Keep the artwork large and centered on a square canvas. Transparent background, including inside every zero. Only the red numerals and two red underlines should be visible; no white backing, extrusion, shadow, colored artifacts or extra marks. Crisp emoji artwork that reads at small sizes.
