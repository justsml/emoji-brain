import fs from 'node:fs/promises';import sharp from 'sharp';import {createHash} from 'node:crypto';import {decodeAnimation} from './animation-frames.mjs';import {openrouterImage} from './openrouter-image.mjs';import {muxAnimation} from './mux-animation.mjs';
const base='experiments/image-enhancement/remaining-animations/severance-running',dir=base+'/nano-reference-v2';await fs.mkdir(dir,{recursive:true});const a=await decodeAnimation('public/emojis/severance-running.webp'),r=JSON.parse(await fs.readFile(base+'/result.json')),files=[],sample=process.argv.includes('--sample');
const ref=await fs.readFile(base+'/source-recovery/milchick-reference.png');
for(let batch=0;batch<(sample?1:6);batch++){
 const layers=[];for(let j=0;j<15;j++)layers.push({input:await sharp(a.frames[batch*15+j],{raw:{width:64,height:64,channels:4}}).resize(128,128).png().toBuffer(),left:j%4*160+16,top:Math.floor(j/4)*160+16});
 const atlas=await sharp({create:{width:640,height:640,channels:3,background:'white'}}).composite(layers).png().toBuffer();await fs.writeFile(dir+'/source-'+batch+'.png',atlas);
 let prompt='Restore the FIRST image: exactly FIFTEEN frames in FOUR columns and FOUR rows, with the last cell blank. The SECOND image contains clean reference stills of the SAME character and hallway from Severance: Seth Milchick, played by Tramell Tillman, Black man with short afro and moustache, short-sleeved white dress shirt, black tie, black trousers and black lanyard. Use it ONLY for photographic appearance and identity. Return ONLY the restored 4-by-4 sheet, not the reference. The first sheet has EXTREME random black/white speckles and mottled compression noise across walls, shirt and skin. Remove ALL this damage. Walls are smooth pale cool gray, shirt clean white, dark skin natural and smooth, ceiling light panels clean white. This is live-action footage, not a painting. Preserve EXACTLY every original frame pose, subject size, location, crop, perspective, direction, head orientation and limb position, including empty hallway frames. Do not add a person to an empty frame. Preserve actual running motion blur; remove compression speckles. Use identical color, lighting and treatment across all frames. Keep every square picture within the exact 10 percent WHITE cell margins, same 4 column / 4 row alignment, no borders, labels, text or extra panels. Do not sharpen into painterly patches or change the actor. Output the full sheet.';
 if(batch>=2)prompt='Restore this exact FOUR by FOUR sprite sheet, FIFTEEN frames with final cell blank. These are WIDE STATIC CAMERA SHOTS of a distant man running AWAY from camera through a white corridor. Preserve the exact small size and silhouette of the person in EVERY frame: never enlarge him or turn him toward camera. Empty frames MUST remain completely empty. The man enters from the LEFT and runs away down the corridor. The dark marks that form his small head and black trousers are real; all the other random tiny speckles across walls and shirt are severe compression damage to remove. Reconstruct smooth clean pale gray walls, white ceiling lights, white floor with dark baseboards, the same perspective and door positions in every cell. His back faces the camera, he wears short sleeve white shirt and black trousers. Do not add a face or closeup. This is real live action, not illustration. Preserve original camera crop, motion blur, subject footprint and exact location. Keep every square image in its exact 10 percent WHITE cell margins. Output the full 4 by 4 sheet with no labels, lines or text. Do not add any person to an empty cell.';
 const out=await openrouterImage({dir,key:(batch>=2?'restored-wide-':'restored-')+batch,prompt,references:batch>=2?['data:image/png;base64,'+atlas.toString('base64')]:['data:image/png;base64,'+atlas.toString('base64'),'data:image/png;base64,'+ref.toString('base64')],resolution:'2K',aspectRatio:'1:1'});const m=await sharp(out.file).metadata();
 for(let j=0;j<15;j++){const cw=m.width/4,ch=m.height/4,file=dir+'/frame-'+(batch*15+j)+'.png';await sharp(out.file).extract({left:Math.round((j%4+.1)*cw),top:Math.round((Math.floor(j/4)+.1)*ch),width:Math.round(.8*cw),height:Math.round(.8*ch)}).resize(512,512).ensureAlpha().png().toFile(file);files.push(file);}console.log('NANO SHEET',batch+1,'/ 6');
}
if(!sample){const receipts=await Promise.all(Array.from({length:6},(_,i)=>fs.readFile(dir+(i>=2?'/restored-wide-':'/restored-')+i+'.openrouter.json').then(JSON.parse)));const candidateFile=dir+'/candidate.webp';await muxAnimation({...r,frameFiles:files},candidateFile);console.log('Trial only: inspect sheets before selecting any output.');}
